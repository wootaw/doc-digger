require 'spec_helper'

describe Restwoods do
  it 'has a version number' do
    expect(Restwoods::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
